# Optional public configuration inputs

This directory records public repositories that can be used to exercise the static configuration extractors. Full third-party repositories are not committed here.

The extraction path is optional and separate from the deterministic local examples. It provides authorization/configuration structure, not production access semantics or traffic.
